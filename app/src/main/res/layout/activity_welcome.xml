<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@drawable/bg_gradient">

    <!-- Cute Mascot / Icon -->
    <ImageView
        android:id="@+id/welcomeImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/dictionary_mascot"
    android:layout_marginBottom="30dp"
    android:contentDescription="Dictionary Mascot" />

    <!-- Stylish Welcome Message -->
    <TextView
        android:id="@+id/welcomeMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hey there, legend! 🚀"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:fontFamily="sans-serif-medium" />

    <!-- Emoji greeting (secondary line) -->
    <TextView
        android:id="@+id/funMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ready to learn some wicked words? 😎📚"
        android:textSize="18sp"
        android:textColor="#E3FDFD"
        android:gravity="center"
        android:layout_marginTop="10dp"
        android:fontFamily="sans-serif-light" />

</LinearLayout>
